---
phase: 04-test-coverage-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/local-store.test.ts
autonomous: true
requirements:
  - REQ-15
user_setup: []
must_haves:
  truths:
    - "local-store.ts functions are tested in isolation with mocked filesystem"
    - "upsertUser merges existing user data correctly without overwriting createdAt"
    - "getUser returns null for non-existent githubId"
    - "addScanRecord appends to scanHistory and write persists to disk"
    - "getScanHistory returns records sorted by timestamp descending"
    - "updateLastScan updates only existing users, skips non-existent"
    - "exportUserData returns combined user + scanHistory"
  artifacts:
    - path: "src/lib/local-store.test.ts"
      provides: "Unit tests for all exports of local-store.ts"
      exports: [describe, it, expect, vi]
  key_links:
    - from: "src/lib/local-store.test.ts"
      to: "src/lib/local-store.ts"
      via: "import all exports"
      pattern: "import.*from.*'./local-store'"
---

<objective>
Write unit tests for src/lib/local-store.ts — file-based persistence layer for user accounts and scan history.

Purpose: Achieve coverage for local-store CRUD operations (REQ-15). This is a file I/O module — tests must mock `fs.promises` to avoid writing to disk.

Output: `src/lib/local-store.test.ts`
</objective>

<execution_context>
@/Users/saiduttaabhishekdash/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/saiduttaabhishekdash/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/local-store.ts

<interfaces>
From src/lib/local-store.ts:

```
// Exports to test:
export async function upsertUser(user: { githubId: number; email: string; name: string; avatarUrl?: string }): Promise<number>
export async function getUser(githubId: number): Promise<StoredUser | null>
export async function updateLastScan(githubId: number): Promise<void>
export async function addScanRecord(record: Omit<ScanRecord, 'timestamp'>): Promise<void>
export async function getScanHistory(githubId: number): Promise<ScanRecord[]>
export async function exportUserData(githubId: number): Promise<{ user: StoredUser | null; scanHistory: ScanRecord[] }>

// Internal types:
interface StoredUser { githubId: number; email: string; name: string; avatarUrl?: string; createdAt: number; updatedAt: number; lastScanAt?: number }
interface ScanRecord { repoName: string; repoUrl: string; scanType: 'quick' | 'deep' | 'batch'; vulnerabilitiesFound: number; criticalCount: number; highCount: number; mediumCount: number; lowCount: number; scanDurationMs: number; timestamp: number; githubId: number }

// Internal functions (tested indirectly):
async function readData(): Promise<StorageData>
async function writeData(data: StorageData): Promise<void>
function defaultData(): StorageData
```

Existing test patterns from codebase:
- Co-located `.test.ts` files next to source
- Tests use `describe/it/expect` from vitest
- `vi.mock('module/path', () => ({ fn: vi.fn() }))` for mocking
- `vi.clearAllMocks()` in beforeEach
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test internal readData/writeData behavior via exported functions</name>
  <files>src/lib/local-store.test.ts</files>
  <behavior>
    - readData returns defaultData when readFile throws ENOENT
    - readData parses and returns valid JSON from readFile
    - writeData calls mkdir with recursive:true then writeFile with JSON-serialized data
  </behavior>
  <action>
    Create the test file at src/lib/local-store.test.ts.

    Mock strategy:
    - Use `vi.mock('fs', () => ({ promises: { readFile: vi.fn(), writeFile: vi.fn(), mkdir: vi.fn() } }))`
    - Use `vi.spyOn(process, 'cwd').mockReturnValue('/tmp/vulscany-test')` to control data directory

    Test scenarios:
    1. readData fallback: mock readFile to reject with ENOENT, call getUser(999) => returns null
    2. readData parses JSON: mock readFile with valid JSON containing a user, getUser(123) returns user
    3. writeData creates directory: mock mkdir and writeFile to resolve, call upsertUser, verify mkdir called with path ending in '/tmp/vulscany-test/.vulscany' and { recursive: true }, verify writeFile called with JSON string
  </action>
  <verify>
    <automated>npx vitest run src/lib/local-store.test.ts --reporter=verbose 2>&1 | head -40</automated>
  </verify>
  <done>I/O-layer tests pass. readData falls back on ENOENT. writeData calls mkdir + writeFile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test user operations — upsertUser, getUser, updateLastScan</name>
  <files>src/lib/local-store.test.ts</files>
  <behavior>
    - upsertUser creates new user with createdAt and updatedAt set
    - upsertUser merges existing user, preserves original createdAt, updates updatedAt
    - getUser returns null for non-existent githubId
    - getUser returns full user object for existing user
    - updateLastScan updates lastScanAt and updatedAt for existing user
    - updateLastScan does nothing for non-existent user (no writeFile call)
  </behavior>
  <action>
    Add tests to the same file for user CRUD operations.

    Use mock fs to control data state per test (mockResolvedValueOnce for readFile in some tests):

    1. upsertUser creates new: When readFile returns empty data, call upsertUser, verify writeFile data contains user with createdAt set
    2. upsertUser merges: Mock readFile with existing user, call upsertUser with different email, verify writeFile data has new email but same createdAt
    3. getUser returns null: Mock readFile with empty data, getUser(999) returns null
    4. getUser returns user: Mock readFile with user data, getUser(1) returns user
    5. updateLastScan updates: Mock readFile with user, call updateLastScan(1), verify lastScanAt and updatedAt changed
    6. updateLastScan skips missing: Mock readFile with empty data, call updateLastScan(999), verify writeFile NOT called
  </action>
  <verify>
    <automated>npx vitest run src/lib/local-store.test.ts --reporter=verbose 2>&1 | head -60</automated>
  </verify>
  <done>All user operation tests pass. upsertUser creates/merges correctly. getUser works. updateLastScan updates only existing users.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test scan history operations — addScanRecord, getScanHistory, exportUserData</name>
  <files>src/lib/local-store.test.ts</files>
  <behavior>
    - addScanRecord appends record with auto-generated timestamp
    - getScanHistory returns only matching user records sorted by timestamp desc
    - getScanHistory returns empty array for user with no scans
    - Multiple addScanRecord calls accumulate records
    - exportUserData returns combined { user, scanHistory }
  </behavior>
  <action>
    Add tests for scan history operations:

    1. addScanRecord appends: Mock readFile with empty data, call addScanRecord, verify writeFile data has scanHistory[0] with timestamp (expect.any(Number))
    2. getScanHistory filters by user: Mock readFile with records for users 1 and 2, getScanHistory(1) returns only githubId=1 records
    3. getScanHistory sorts desc: Mock readFile with 2 records for same user with different timestamps, getScanHistory(1) returns most recent first
    4. getScanHistory empty: Mock readFile with empty data, getScanHistory(999) returns []
    5. Multiple addScanRecord calls: Call addScanRecord 3 times, verify scanHistory length is 3
    6. exportUserData bundles: Mock readFile with user + records, exportUserData(1) returns { user, scanHistory }
  </action>
  <verify>
    <automated>npx vitest run src/lib/local-store.test.ts --reporter=verbose 2>&1</automated>
  </verify>
  <done>All scan history tests pass. Records append. getScanHistory filters + sorts. exportUserData returns bundle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test code -> fs mock | No real I/O — filesystem fully mocked. No test pollution. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | fs mock leakage across tests | mitigate | vi.clearAllMocks() in beforeEach ensures clean state |
| T-04-01-02 | Information Disclosure | Test output paths | accept | /tmp paths are ephemeral, no real secrets used |
</threat_model>

<verification>
```
npx vitest run src/lib/local-store.test.ts --reporter=verbose
```
Expected: All tests pass, no type errors, no real I/O.
</verification>

<success_criteria>
- src/lib/local-store.test.ts exists with tests for all 6 exported functions
- Tests use mocked fs.promises — no real disk I/O
- All tests pass under vitest run
</success_criteria>
