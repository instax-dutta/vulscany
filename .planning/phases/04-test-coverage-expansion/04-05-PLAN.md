---
phase: 04-test-coverage-expansion
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/threat-intel/index.test.ts
autonomous: true
requirements:
  - REQ-16
  - REQ-19
user_setup: []
must_haves:
  truths:
    - "hashDependencies produces deterministic 12-char hex hash for same deps"
    - "Different dependency sets produce different hashes"
    - "hashDependencies sorts keys alphabetically before hashing"
    - "getLatestThreats returns cached data when available"
    - "getLatestThreats fetches fresh data on cache miss"
    - "analyzeRepositoryThreats uses hashDependencies for cache key"
    - "getPackageVulnerabilities returns PackageVulnerability for a given package+version"
    - "prewarmThreatCache handles errors gracefully without throwing"
  artifacts:
    - path: "src/lib/threat-intel/index.test.ts"
      provides: "Unit tests for index.ts orchestrator + hashDependencies"
  key_links:
    - from: "index.test.ts"
      to: "src/lib/threat-intel/index.ts"
      via: "import all exports"
    - from: "index.test.ts"
      to: "src/lib/threat-intel/memory-cache.ts"
      via: "vi.mock for cache layer"
---

<objective>
Write unit tests for src/lib/threat-intel/index.ts — the orchestrator module that ties together memory-cache, cve-fetcher, github-advisories, and risk-analyzer. Also tests the hashDependencies function (REQ-19).

Purpose: Verify the threat-intel orchestration layer works correctly — cache-first strategy, fallback to fresh fetches, dependency hashing for cache keys, and error handling in prewarmThreatCache (REQ-16 part c + REQ-19).

Output: `src/lib/threat-intel/index.test.ts`
</objective>

<execution_context>
@/Users/saiduttaabhishekdash/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/saiduttaabhishekdash/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/threat-intel/index.ts
@src/lib/threat-intel/types.ts

<interfaces>
From src/lib/threat-intel/index.ts:

```
// Single exported function hashDependencies (line 122-125) — NOT exported but used internally
function hashDependencies(deps: Record<string, string>): string {
    const sorted = Object.keys(deps).sort().map(k => `${k}@${deps[k]}`).join(',');
    const hash = crypto.createHash('sha256').update(sorted).digest('hex');
    return hash.substring(0, 12);
}

// Exported functions:
export async function getLatestThreats(): Promise<{ cves: CVEData[]; advisories: GitHubAdvisory[]; lastUpdated: string }>
export async function analyzeRepositoryThreats(dependencies: Record<string, string>): Promise<ThreatIntelligence>
export async function getPackageVulnerabilities(packageName: string, version: string): Promise<PackageVulnerability>
export async function prewarmThreatCache(): Promise<void>

// Internal dependencies (all mocked in tests):
// - fetchReactCVEs from './cve-fetcher'
// - fetchReactAdvisories from './github-advisories'
// - generateThreatIntelligence from './risk-analyzer'
// - analyzePackageRisk from './risk-analyzer'
// - getCachedThreatData, setCachedThreatData from './memory-cache'
// - crypto.createHash from 'crypto' (for hashDependencies)
```

From types.ts:
```
interface CVEData { id: string; description: string; severity: 'LOW'|'MEDIUM'|'HIGH'|'CRITICAL'; cvssScore: number; ... }
interface GitHubAdvisory { id: string; summary: string; severity: 'LOW'|'MODERATE'|'HIGH'|'CRITICAL'; ... }
interface ThreatIntelligence { cveMatches: CVEData[]; advisoryMatches: GitHubAdvisory[]; riskScore: number; riskLevel: 'LOW'|'MEDIUM'|'HIGH'|'CRITICAL'; threatSummary: string; recommendations: string[]; lastUpdated: string }
interface PackageVulnerability { packageName: string; currentVersion: string; vulnerabilities: CVEData[]; advisories: GitHubAdvisory[]; riskScore: number; recommendedVersion?: string }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test hashDependencies pure function</name>
  <files>src/lib/threat-intel/index.test.ts</files>
  <behavior>
    - hashDependencies is deterministic: same input produces same output
    - Different inputs produce different hashes
    - Sorting: { b: '2', a: '1' } produces same hash as { a: '1', b: '2' }
    - Output is 12-character hex string
    - Empty object produces a valid hash
    - Whitespace/stability: same key-value pairs produce same hash regardless of object creation
  </behavior>
  <action>
    Create test file at src/lib/threat-intel/index.test.ts.

    hashDependencies is NOT exported from index.ts. To test it, there are two approaches:
    
    **Recommended approach**: Export hashDependencies from index.ts by adding `export` before the function declaration. This is a test-harness change that doesn't affect the public API surface (it was already implicitly accessible via the internal module system). Add the export:
    ```
    export function hashDependencies(deps: Record<string, string>): string {
    ```
    
    Alternative (if export change is not desired): Test hashDependencies indirectly via `analyzeRepositoryThreats` by inspecting which cache key is used. But direct testing is cleaner.

    The function uses `crypto.createHash('sha256')`. This is a Node.js built-in and works in vitest's node environment without mocking. No vi.mock needed for crypto.

    Test scenarios:
    1. Deterministic: hashDependencies({ react: '18.0.0', next: '14.0.0' }) returns same value when called twice
    2. Different inputs differ: hashDependencies({ react: '18.0.0' }) != hashDependencies({ react: '19.0.0' })
    3. Sorting: hashDependencies({ b: '2', a: '1' }) equals hashDependencies({ a: '1', b: '2' })
    4. Output format: result is string, length 12, matches /^[0-9a-f]{12}$/
    5. Empty object: hashDependencies({}) returns a 12-char hex string (crypto.createHash('sha256').update('').digest('hex').substring(0,12))
    6. Package@version format: The sorted string passed to crypto should be of the form 'react@18.0.0,next@14.0.0' (alphabetically sorted, comma-separated)
  </action>
  <verify>
    <automated>npx vitest run src/lib/threat-intel/index.test.ts --reporter=verbose 2>&1 | head -40</automated>
  </verify>
  <done>hashDependencies tests pass. Deterministic, different inputs differ, sorting stable, output is 12-char hex. Empty object works.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test orchestrator functions — getLatestThreats, analyzeRepositoryThreats, getPackageVulnerabilities</name>
  <files>src/lib/threat-intel/index.test.ts</files>
  <behavior>
    - getLatestThreats returns cached data when cache hit
    - getLatestThreats fetches fresh data on cache miss and caches result
    - analyzeRepositoryThreats uses hashDependencies in cache key
    - analyzeRepositoryThreats returns cached intelligence on hit
    - getPackageVulnerabilities returns PackageVulnerability with packageName and currentVersion
    - prewarmThreatCache runs without throwing on errors
  </behavior>
  <action>
    Add tests to the same file for the orchestrator functions.

    Mock all 4 internal dependencies:
    ```
    vi.mock('./memory-cache', () => ({
      getCachedThreatData: vi.fn(),
      setCachedThreatData: vi.fn(),
    }));
    vi.mock('./cve-fetcher', () => ({ fetchReactCVEs: vi.fn() }));
    vi.mock('./github-advisories', () => ({ fetchReactAdvisories: vi.fn() }));
    vi.mock('./risk-analyzer', () => ({
      generateThreatIntelligence: vi.fn(),
      analyzePackageRisk: vi.fn(),
    }));
    ```

    Define mock data inline:
    ```
    const mockCVEs = [{ id: 'CVE-2024-0001', severity: 'HIGH', cvssScore: 7.5 } as CVEData];
    const mockAdvisories = [{ id: 'GHSA-xxxx', severity: 'HIGH', summary: 'Test' } as GitHubAdvisory];
    const mockThreatIntel: ThreatIntelligence = { cveMatches: mockCVEs, advisoryMatches: mockAdvisories, riskScore: 50, riskLevel: 'HIGH', threatSummary: 'Test', recommendations: ['Fix it'], lastUpdated: new Date().toISOString() };
    const mockPackageVuln: PackageVulnerability = { packageName: 'react', currentVersion: '18.0.0', vulnerabilities: mockCVEs, advisories: mockAdvisories, riskScore: 30 };
    ```

    Test scenarios:
    1. getLatestThreats — cache hit: Mock getCachedThreatData to return { cves: mockCVEs, advisories: mockAdvisories, lastUpdated: '2024-01-01' }. getLatestThreats() returns cached data directly without calling fetchReactCVEs/fetchReactAdvisories.
    2. getLatestThreats — cache miss: Mock getCachedThreatData to return null. Mock fetchReactCVEs to return mockCVEs, fetchReactAdvisories to return mockAdvisories. getLatestThreats() fetches fresh data and calls setCachedThreatData with 'threats:latest:react' key and 21600 TTL.
    3. analyzeRepositoryThreats — cache hit: Mock getCachedThreatData to return mockThreatIntel. analyzeRepositoryThreats({ react: '18.0.0' }) returns mockThreatIntel without calling generateThreatIntelligence.
    4. analyzeRepositoryThreats — cache miss: Mock getCachedThreatData to return null, generateThreatIntelligence to return mockThreatIntel. analyzeRepositoryThreats({ react: '18.0.0' }) calls generateThreatIntelligence with the deps, caches with 'threats:repo:' prefix + hash, 3600 TTL.
    5. getPackageVulnerabilities — cache miss: Mock getCachedThreatData(null), analyzePackageRisk returns mockPackageVuln. getPackageVulnerabilities('react', '18.0.0') returns PackageVulnerability with packageName='react', currentVersion='18.0.0', caches with 'threats:package:react:18.0.0' and 43200 TTL.
    6. prewarmThreatCache — error handling: Mock getLatestThreats to reject. prewarmThreatCache() should NOT throw (error caught by internal .catch()). Verify using await expect(prewarmThreatCache()).resolves.toBeUndefined().

    Note: prewarmThreatCache has a `typeof window !== 'undefined'` guard that returns early in browser. In Node.js vitest environment (`environment: 'node'` in vitest.config.ts), this guard should not fire. If it does, stub `globalThis.window` to undefined using `vi.stubGlobal('window', undefined)`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/threat-intel/index.test.ts --reporter=verbose 2>&1</automated>
  </verify>
  <done>Orchestrator tests pass. Cache hit/miss logic works. analyzeRepositoryThreats uses hash for cache key. prewarmThreatCache nothrows on error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test -> mocked modules | All HTTP and cache dependencies are mocked |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | Crypto module in test | accept | crypto.createHash is a pure function, no side effects |
| T-04-05-02 | Information Disclosure | Hash collisions | accept | 12-char hex (48 bits) is sufficient for cache key uniqueness |
</threat_model>

<verification>
```
npx vitest run src/lib/threat-intel/index.test.ts --reporter=verbose
```
Expected: All tests pass, cache-orchestration logic verified, hashDependencies produces correct output.
</verification>

<success_criteria>
- hashDependencies function exported from index.ts (or tested indirectly)
- hashDependencies tests: deterministic, sorting-stable, 12-char hex output
- Orchestrator tests: cache hit/miss for all 3 main functions
- prewarmThreatCache error handling verified
- All tests pass under vitest run
</success_criteria>
