---
phase: 04-test-coverage-expansion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/threat-intel/memory-cache.test.ts
  - src/lib/threat-intel/cve-fetcher.test.ts
autonomous: true
requirements:
  - REQ-16
user_setup: []
must_haves:
  truths:
    - "memory-cache set/get/delete operations work correctly with TTL expiry"
    - "Expired cache entries return null"
    - "getMultipleCachedThreatData returns array with null for expired keys"
    - "getCachedKeys supports wildcard pattern matching"
    - "cve-fetcher makes HTTP requests to NVD API and parses CVEData"
    - "NVD API errors are caught and return empty array"
    - "parseCVEItem extracts severity, score, affectedPackages from raw NVD response"
  artifacts:
    - path: "src/lib/threat-intel/memory-cache.test.ts"
      provides: "Unit tests for memory-cache module"
      exports: [describe, it, expect, vi]
    - path: "src/lib/threat-intel/cve-fetcher.test.ts"
      provides: "Unit tests for cve-fetcher module with MSW HTTP mocking"
      exports: [describe, it, expect, vi, server]
  key_links:
    - from: "memory-cache.test.ts"
      to: "src/lib/threat-intel/memory-cache.ts"
      via: "import all exports"
      pattern: "import.*from.*'./memory-cache'"
    - from: "cve-fetcher.test.ts"
      to: "src/lib/threat-intel/cve-fetcher.ts"
      via: "import all exports"
      pattern: "import.*from.*'./cve-fetcher'"
    - from: "cve-fetcher.test.ts"
      to: "vitest.setup.ts"
      via: "MSW server for HTTP mocking"
      pattern: "import.*server.*from.*vitest.setup"
---

<objective>
Write unit tests for threat-intel submodules: memory-cache (in-memory cache with TTL) and cve-fetcher (NVD API client).

Purpose: Cover threat-intel caching layer and external API fetching with HTTP mocking (REQ-16 part a). The memory-cache is purely synchronous logic (despite async API), and cve-fetcher requires MSW to mock the NVD REST API.

Output: `src/lib/threat-intel/memory-cache.test.ts`, `src/lib/threat-intel/cve-fetcher.test.ts`
</objective>

<execution_context>
@/Users/saiduttaabhishekdash/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/saiduttaabhishekdash/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/threat-intel/memory-cache.ts
@src/lib/threat-intel/cve-fetcher.ts
@src/lib/threat-intel/types.ts

<interfaces>
From memory-cache.ts:
```
export async function getCachedThreatData(key: string): Promise<any | null>
export async function getMultipleCachedThreatData(keys: string[]): Promise<any[]>
export async function setCachedThreatData(key: string, data: any, ttlSeconds?: number): Promise<void>
export async function deleteCachedThreatData(key: string): Promise<void>
export async function getCachedKeys(pattern: string): Promise<string[]>
// Also: setInterval cleanup every 3600000ms (1 hour)
```

From cve-fetcher.ts:
```
export async function fetchReactCVEs(limit?: number): Promise<CVEData[]>
export async function fetchCVEsByPackage(packageName: string): Promise<CVEData[]>
// Internal: function parseCVEItem(cve: any): CVEData
// Uses: NVD_API_BASE = 'https://services.nvd.nist.gov/rest/json/cves/2.0'
// Makes fetch() calls with signal: AbortSignal.timeout(10000)
```

From types.ts:
```
interface CVEData { id: string; description: string; severity: 'LOW'|'MEDIUM'|'HIGH'|'CRITICAL'; cvssScore: number; cvssVector?: string; publishedDate: string; lastModifiedDate: string; affectedPackages: string[]; references: string[]; cweIds?: string[]; exploitAvailable?: boolean; patchAvailable?: boolean; }
```

Existing MSW pattern (from fix-generator.test.ts):
```
import { server } from '../../../vitest.setup';
import { http, HttpResponse } from 'msw';
server.use(http.post('https://api.mistral.ai/v1/chat/completions', () => { return HttpResponse.json({...}); }));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test memory-cache set/get/delete/expiry/pattern operations</name>
  <files>src/lib/threat-intel/memory-cache.test.ts</files>
  <behavior>
    - setCachedThreatData stores data, getCachedThreatData retrieves it
    - Expired cache entries return null (advance timers past TTL)
    - deleteCachedThreatData removes entry
    - getMultipleCachedThreatData returns mixed array of data and nulls
    - getCachedKeys supports '*' wildcard pattern matching
  </behavior>
  <action>
    Create test file at src/lib/threat-intel/memory-cache.test.ts.

    The memory-cache module uses a module-level Map and setInterval. Because the Map is shared across tests in the same module, each test must start with a clean cache. Use `beforeEach` to clear the internal state. The module doesn't export a clear function, so the approach is:

    1. Use `vi.useFakeTimers()` for TTL control
    2. For cache cleanup between tests, rely on fresh imports per test via `vi.resetModules()` + dynamic import, OR simply test within the same describe block with careful state management. The module-level Map is the challenge.

    Recommended approach: Since we can't access the module-level Map directly, we can use getCachedKeys('*') after operations to verify state, and rely on the fact that each test file loads fresh module state when vitest runs it.

    Test scenarios:
    1. Set and get: setCachedThreatData('key1', { data: 'value' }, 3600) then getCachedThreatData('key1') returns { data: 'value' }
    2. Get non-existent key: getCachedThreatData('nonexistent') returns null
    3. Expired entry: set TTL=1 second, advance 2 seconds with vi.advanceTimersByTime(2000), get returns null
    4. Delete: Set a key, delete it, get returns null
    5. Multiple get: Set 2 keys, getMultipleCachedThreatData(['key1', 'key2', 'bad']) returns [data, data, null]
    6. Key pattern: Set keys 'threats:latest:react', 'threats:repo:abc', 'users:123'. getCachedKeys('threats:*') returns 2 keys. getCachedKeys('*:latest:*') returns 1 key.
    7. Default TTL is 86400: Set without ttl, advance 12 hours (still valid), advance past 24 hours (expired)

    Note: The setInterval cleanup runs every hour. Since we use fake timers, this may fire. Ignore it in tests or mock/unmock as needed.
  </action>
  <verify>
    <automated>npx vitest run src/lib/threat-intel/memory-cache.test.ts --reporter=verbose 2>&1 | head -50</automated>
  </verify>
  <done>Memory-cache tests pass. Set/get/delete work. Expired entries return null. Pattern matching works. Multiple get handles mixed results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test cve-fetcher — NVD API requests with MSW mocking</name>
  <files>src/lib/threat-intel/cve-fetcher.test.ts</files>
  <behavior>
    - fetchReactCVEs makes request to NVD API and parses response into CVEData[]
    - NVD API error (500) returns empty array
    - fetchCVEsByPackage makes request for specific package and parses response
    - parseCVEItem extracts severity, cvssScore, affectedPackages from raw CVE JSON
  </behavior>
  <action>
    Create test file at src/lib/threat-intel/cve-fetcher.test.ts.

    Mock strategy: Use MSW (already set up in vitest.setup.ts) to intercept NVD API calls:
    ```
    import { server } from '../../../vitest.setup';
    import { http, HttpResponse } from 'msw';
    ```

    Mock NVD API response structure (simplified):
    ```
    { vulnerabilities: [{ cve: { id: 'CVE-2024-12345', descriptions: [{ lang: 'en', value: 'Test CVE' }], metrics: { cvssMetricV31: [{ cvssData: { baseSeverity: 'HIGH', baseScore: 7.5, vectorString: 'CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N' } }] }, published: '2024-01-01T00:00Z', lastModified: '2024-06-01T00:00Z', references: [{ url: 'https://example.com' }], weaknesses: [{ description: [{ value: 'CWE-79' }] }], configurations: [{ nodes: [{ cpeMatch: [{ criteria: 'cpe:2.3:a:facebook:react:18.0.0:*:*:*:*:*:*:*' }] }] }] }] }
    ```

    Test scenarios:
    1. fetchReactCVEs success: MSW intercepts GET to NVD_API_BASE with keywordSearch=react, returns mock CVE data. fetchReactCVEs(5) returns array of CVEData objects with correct id, severity, score, affectedPackages.
    2. fetchReactCVEs respects limit: Mock 10 CVEs, fetchReactCVEs(3) returns array of length 3 (confirm slice logic).
    3. Deduplication: Mock identical CVE IDs from different keyword searches. fetchReactCVEs should dedup by ID.
    4. NVD API error: MSW returns 500. fetchReactCVEs(5) returns empty array (error caught by try/catch).
    5. fetchCVEsByPackage: MSW intercepts keywordSearch=express, returns mock data. fetchCVEsByPackage('express') returns CVEData[]. Verify id and severity.

    Mock the memory-cache dependency:
    - Use `vi.mock('../threat-intel/memory-cache', () => ({ getCachedThreatData: vi.fn().mockResolvedValue(null), setCachedThreatData: vi.fn().mockResolvedValue(undefined) }))`
    - This ensures cache is always missed, forcing real NVD API calls.

    Note on URL matching: MSW intercepts based on URL. The NVD API URL includes keywordSearch parameter. Use `http.get('https://services.nvd.nist.gov/rest/json/cves/2.0')` with a function that checks the URL params, OR match exactly with the URL including query string.
  </action>
  <verify>
    <automated>npx vitest run src/lib/threat-intel/cve-fetcher.test.ts --reporter=verbose 2>&1 | head -60</automated>
  </verify>
  <done>CVE-fetcher tests pass. MSW mocks NVD API. Successful responses parse into CVEData. Errors return empty array. fetchCVEsByPackage works. Deduplication works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test -> MSW HTTP mock | No real HTTP requests — MSW intercepts at the fetch() level |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Spoofing | MSW handler not matching | mitigate | Test URL patterns match actual API endpoints used by cve-fetcher |
| T-04-03-02 | Information Disclosure | Cache state leakage between tests | mitigate | Each test manages its own state; memory-cache is module-isolated per test file |
</threat_model>

<verification>
```
npx vitest run src/lib/threat-intel/memory-cache.test.ts src/lib/threat-intel/cve-fetcher.test.ts --reporter=verbose
```
Expected: All tests pass, MSW intercepts NVD API calls, memory-cache TTL works correctly.
</verification>

<success_criteria>
- memory-cache.test.ts tests all 5 exported functions with TTL expiry
- cve-fetcher.test.ts tests both exported functions with MSW HTTP mocking
- parseCVEItem tested indirectly through fetchReactCVEs
- Deduplication and limit logic verified
- All tests pass under vitest run
</success_criteria>
