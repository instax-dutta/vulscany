---
phase: 04-test-coverage-expansion
plan: 06
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
  - 04-05
files_modified:
  - src/app/api/scan/scan-fix-integration.test.ts
autonomous: true
requirements:
  - REQ-20
user_setup: []
must_haves:
  truths:
    - "Full scan flow: authenticated POST /api/scan with owner/repo returns scan result with threat intel"
    - "Error path: POST /api/scan without auth returns 401"
    - "Error path: POST /api/scan with missing owner/repo returns 400"
    - "Error path: POST /api/scan to non-web-app returns 400"
    - "Fix flow: POST /api/ai/generate-pr with vulnerabilities returns fixes with diffs"
    - "Fix flow without auth returns 401"
    - "Scan result includes threatIntelligence when dependencies present"
  artifacts:
    - path: "src/app/api/scan/scan-fix-integration.test.ts"
      provides: "Integration test for scan -> fix pipeline"
  key_links:
    - from: "scan-fix-integration.test.ts"
      to: "src/app/api/scan/route.ts"
      via: "import POST handler"
    - from: "scan-fix-integration.test.ts"
      to: "src/app/api/ai/generate-pr/route.ts"
      via: "import POST handler"
    - from: "scan-fix-integration.test.ts"
      to: "vitest.setup.ts"
      via: "MSW server for HTTP mocking"
---

<objective>
Write integration test for the scan-fix pipeline — the full flow from scanning a repository through the generate-pr endpoint.

Purpose: Verify the scan route integrates with threat-intel (for risk scoring), local-store (for scan history), scan-cache (for result caching), and that the generate-pr route consumes scan results to produce code fixes (REQ-20).

Note: This is Wave 2 because it depends on understanding the patterns established in Wave 1 test files. The executor should read the relevant PLAN SUMMARYs before starting.

Output: `src/app/api/scan/scan-fix-integration.test.ts`
</objective>

<execution_context>
@/Users/saiduttaabhishekdash/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/saiduttaabhishekdash/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/api/scan/route.ts
@src/app/api/ai/generate-pr/route.ts
@src/lib/scanner/index.ts
@src/lib/ai/fix-generator.ts
@src/app/api/scan/scan.test.ts
@src/app/api/batch-scan/batch-scan.test.ts

<interfaces>
From src/app/api/scan/route.ts:
```
export async function GET(request: NextRequest) — returns repositories list
export async function POST(request: NextRequest) — main scan endpoint
// Imports: cookies(), fetchUserRepositories, detectStack, scanRepository, rateLimit
// Dynamic imports: getCachedScanResult, cacheScanResult, invalidateScanCache, analyzeRepositoryThreats, addScanRecord, updateLastScan
```

From src/app/api/ai/generate-pr/route.ts:
```
export async function POST(request: NextRequest) — generates PR with fixes
// Accepts: { owner, repo, vulnerabilities, baseBranch?, mode? }
// Imports: cookies(), generateBatchFixes, createSecurityFixPR, getFileContent, validateGeneratedCode
```

From src/lib/scanner/index.ts:
```
interface Vulnerability { id: string; type: string; severity: 'low'|'medium'|'high'|'critical'; title: string; description: string; file: string; line?: number; snippet?: string; recommendation: string }
interface ScanResult { repoName: string; owner: string; scanTimestamp: string; stackInfo: any; vulnerabilities: Vulnerability[]; status: string; summary: string; scanDuration: number; threatIntelligence?: {...} }
```

Existing test patterns (from scan.test.ts):
```
vi.mock('next/headers', () => ({ cookies: vi.fn() }));
vi.mock('@/lib/scanner', () => ({ scanRepository: vi.fn() }));
vi.mock('@/lib/cache/scan-cache', () => ({ getCachedScanResult: vi.fn().mockResolvedValue(null), cacheScanResult: vi.fn().mockResolvedValue(true) }));
vi.mock('@/lib/local-store', () => ({ addScanRecord: vi.fn(), updateLastScan: vi.fn() }));
vi.mock('@/lib/rate-limit', () => ({ rateLimit: vi.fn().mockResolvedValue({ success: true, limit: 10, remaining: 9, reset: 0 }) }));
```

Existing test patterns (from fix-generator.test.ts):
```
import { server } from '../../../vitest.setup';
import { http, HttpResponse } from 'msw';
server.use(http.post('https://api.mistral.ai/v1/chat/completions', () => { return HttpResponse.json({...}); }));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scan endpoint integration — full happy path with threat intel and local-store</name>
  <files>src/app/api/scan/scan-fix-integration.test.ts</files>
  <behavior>
    - Authenticated POST /api/scan with valid owner/repo returns 200
    - Response includes scanResult with vulnerabilities, status, summary, scanDuration
    - Response includes threatIntelligence when dependencies present
    - addScanRecord and updateLastScan called for authenticated user
    - Rate limited request returns 429
    - Cache hit returns cached result without re-scanning
  </behavior>
  <action>
    Create the integration test file. This tests the scan route with all its real dependencies mocked.

    Mock strategy (matching existing scan.test.ts patterns):
    ```
    vi.mock('next/headers', () => ({ cookies: vi.fn() }));
    vi.mock('@/lib/scanner', () => ({ scanRepository: vi.fn() }));
    vi.mock('@/lib/github/stack-detector', () => ({ detectStack: vi.fn() }));
    vi.mock('@/lib/cache/scan-cache', () => ({ getCachedScanResult: vi.fn().mockResolvedValue(null), cacheScanResult: vi.fn().mockResolvedValue(true), invalidateScanCache: vi.fn() }));
    vi.mock('@/lib/local-store', () => ({ addScanRecord: vi.fn(), updateLastScan: vi.fn() }));
    vi.mock('@/lib/rate-limit', () => ({ rateLimit: vi.fn().mockResolvedValue({ success: true, limit: 10, remaining: 9, reset: 0 }) }));
    ```

    For threat-intel, the scan route uses dynamic import `await import('@/lib/threat-intel')`. This means vi.mock of `@/lib/threat-intel` will be picked up by the dynamic import if registered before the route is first imported.

    ```
    vi.mock('@/lib/threat-intel', () => ({ analyzeRepositoryThreats: vi.fn() }));
    ```

    Test scenarios:
    1. Full happy path: Mock cookies to return github_token and session with user.id. Mock detectStack to return { stack: 'react', dependencies: { react: '18.0.0' } }. Mock scanRepository to return ScanResult with 2 vulnerabilities. Mock analyzeRepositoryThreats to return ThreatIntelligence with riskScore. Create POST request with { owner: 'test', repo: 'test', force: false }.
       - Expect 200 response
       - Response body has scanResult with vulnerabilities, status, summary, scanDuration, threatIntelligence
       - threatIntelligence has riskScore and riskLevel
       - addScanRecord was called with correct githubId and vulnerability counts
       - updateLastScan was called with correct githubId

    2. Cache hit: Mock getCachedScanResult to return a cached ScanResult. Call POST. Expect 200, body.cached=true, body.scanResult equals cached data. scanRepository NOT called.

    3. Cache hit with force: Mock getCachedScanResult to return cached data. Call POST with force:true. invalidateScanCache is called, then scan proceeds as normal (cache bypassed).

    4. Rate limited: Mock rateLimit to return { success: false }. Call POST. Expect 429 response with error message.

    5. Non-web app: Mock detectStack to return null. Call POST. Expect 400 with error message about not being a web application.

    Use `vi.clearAllMocks()` in beforeEach.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/scan/scan-fix-integration.test.ts --reporter=verbose 2>&1 | head -80</automated>
  </verify>
  <done>Scan integration tests pass. Happy path returns scanResult with threatIntelligence. Auth/input errors return correct codes. Cache hit returns cached data without re-scanning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test generate-pr endpoint integration</name>
  <files>src/app/api/scan/scan-fix-integration.test.ts</files>
  <behavior>
    - Authenticated POST /api/ai/generate-pr with owner/repo/vulnerabilities returns fixes with diffs
    - Unauthenticated POST returns 401
    - Missing fields return 400
    - generateBatchFixes returns fixes with filePath, diff, commitMessage
    - Code validation results included in preview mode response
  </behavior>
  <action>
    Add generate-pr integration tests to the same file.

    Mock strategy:
    ```
    vi.mock('@/lib/ai/fix-generator', () => ({ generateBatchFixes: vi.fn() }));
    vi.mock('@/lib/github/pr-creator', () => ({ createSecurityFixPR: vi.fn() }));
    vi.mock('@/lib/github/client', () => ({ getFileContent: vi.fn() }));
    vi.mock('@/lib/ai/pr-context', () => ({ getProjectContext: vi.fn(), buildContextString: vi.fn() }));
    vi.mock('@/lib/validators/code-validator', () => ({ validateGeneratedCode: vi.fn() }));
    vi.mock('@/lib/user/stats', () => ({ incrementUserMetric: vi.fn() }));
    ```

    Define mock fixtures:
    ```
    const mockVulnerabilities = [{ id: 'vuln-1', type: 'dangerous-api', severity: 'high', title: 'Unsafe HTML', description: 'test', file: 'src/Component.tsx', line: 10, snippet: '...', recommendation: 'Fix it' }];
    const mockFixResult = { filePath: 'src/Component.tsx', vulnerabilityId: 'vuln-1', fixedCode: 'import React...', diff: '- old\n+ new', commitMessage: 'fix(security): Unsafe HTML' };
    const mockValidation = { valid: true, errors: [], warnings: [] };
    ```

    Test scenarios:
    1. Happy path — preview mode: Mock cookies to return github_token. Mock getFileContent to return { content: 'source code' }. Mock getProjectContext/buildContextString to return context. Mock generateBatchFixes to return [mockFixResult]. Mock validateGeneratedCode to return mockValidation. Create POST with { owner: 'test', repo: 'test', vulnerabilities: mockVulnerabilities, mode: 'preview' }.
       - Expect 200 response
       - Response mode='preview'
       - fixes array contains fix with filePath, diff, commitMessage, validation
       - validationSummary present

    2. Unauthenticated: Mock cookies to return undefined for github_token. Expect 401 response with error field.

    3. Missing owner: POST with { vulnerabilities: [] }. Expect 400.

    4. Missing vulnerabilities: POST with { owner: 'test', repo: 'test' }. Expect 400.

    5. generateBatchFixes returns empty: Mock generateBatchFixes to return []. Expect 500 response with 'No fixes could be generated'.

    Note: The route uses `export const runtime = 'nodejs'` and `export const dynamic = 'force-dynamic'`. These export declarations won't interfere with tests but should be noted.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/scan/scan-fix-integration.test.ts --reporter=verbose 2>&1</automated>
  </verify>
  <done>Generate-pr integration tests pass. Preview mode returns fixes. Auth/validation errors return correct codes. Empty fixes handled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test -> mocked HTTP/API | All external calls (GitHub, Mistral) are mocked via vi.mock or MSW |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | Mock leakage across tests | mitigate | vi.clearAllMocks() in beforeEach ensures clean state |
| T-04-06-02 | Information Disclosure | Dynamic import mocking | accept | vi.mock intercepts dynamic imports — proven pattern in scan.test.ts |
</threat_model>

<verification>
```
npx vitest run src/app/api/scan/scan-fix-integration.test.ts --reporter=verbose
```
Expected: All tests pass, scan and generate-pr endpoints integrate correctly with mocked dependencies.
</verification>

<success_criteria>
- Scan endpoint: full happy path, auth errors, missing fields, non-web-app, rate limiting, cache hit
- Generate-pr endpoint: happy path (preview mode), auth errors, missing fields, empty fixes
- All tests pass under vitest run
</success_criteria>
