---
phase: 04-test-coverage-expansion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/threat-intel/github-advisories.test.ts
  - src/lib/threat-intel/risk-analyzer.test.ts
autonomous: true
requirements:
  - REQ-16
user_setup: []
must_haves:
  truths:
    - "github-advisories fetches from GitHub API and parses GitHubAdvisory type"
    - "GitHub API errors are caught and return empty array"
    - "parseAdvisory extracts severity, summary, vulnerabilities from raw response"
    - "risk-analyzer calculates package risk scores from CVEs and advisories"
    - "calculatePackageRiskScore assigns points by severity with exploit bonus"
    - "calculateOverallRiskScore computes composite score from risk factors"
    - "getRiskLevel maps 0-100 score to LOW/MEDIUM/HIGH/CRITICAL"
    - "generateThreatSummary and generateRecommendations produce human-readable output"
  artifacts:
    - path: "src/lib/threat-intel/github-advisories.test.ts"
      provides: "Unit tests for github-advisories with MSW HTTP mocking"
    - path: "src/lib/threat-intel/risk-analyzer.test.ts"
      provides: "Unit tests for risk-analyzer pure logic"
  key_links:
    - from: "github-advisories.test.ts"
      to: "src/lib/threat-intel/github-advisories.ts"
      via: "import all exports"
    - from: "github-advisories.test.ts"
      to: "vitest.setup.ts"
      via: "MSW server"
    - from: "risk-analyzer.test.ts"
      to: "src/lib/threat-intel/risk-analyzer.ts"
      via: "import analyzePackageRisk, generateThreatIntelligence"
---

<objective>
Write unit tests for github-advisories (GitHub Security Advisory API client) and risk-analyzer (risk scoring engine).

Purpose: Cover the GitHub Advisory API fetching with MSW mocking and the pure logic risk scoring functions (REQ-16 part b). The risk-analyzer's internal functions (calculatePackageRiskScore, calculateRiskFactors, calculateOverallRiskScore, getRiskLevel, generateThreatSummary, generateRecommendations, isVersionAffected, findSafeVersion) are deterministic and can be tested with hardcoded CVEData/GitHubAdvisory fixtures.

Output: `src/lib/threat-intel/github-advisories.test.ts`, `src/lib/threat-intel/risk-analyzer.test.ts`
</objective>

<execution_context>
@/Users/saiduttaabhishekdash/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/saiduttaabhishekdash/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/threat-intel/github-advisories.ts
@src/lib/threat-intel/risk-analyzer.ts
@src/lib/threat-intel/types.ts

<interfaces>
From github-advisories.ts:
```
export async function fetchReactAdvisories(limit?: number): Promise<GitHubAdvisory[]>
export async function fetchAdvisoriesForPackage(packageName: string): Promise<GitHubAdvisory[]>
export async function searchAdvisoriesByKeyword(keyword: string): Promise<GitHubAdvisory[]>
// Internal: function parseAdvisory(advisory: any): GitHubAdvisory
// Uses: GITHUB_API_BASE = 'https://api.github.com'
// Makes fetch() to /advisories?ecosystem=npm&affects=... with Accept header
```

From risk-analyzer.ts:
```
export async function analyzePackageRisk(packageName: string, currentVersion: string): Promise<PackageVulnerability>
export async function generateThreatIntelligence(dependencies: Record<string, string>): Promise<ThreatIntelligence>
// Internal (test directly):
function calculatePackageRiskScore(cves: CVEData[], advisories: GitHubAdvisory[], currentVersion: string): number
function calculateRiskFactors(cves: CVEData[], advisories: GitHubAdvisory[], dependencies: Record<string, string>): RiskFactors
function calculateOverallRiskScore(factors: RiskFactors): number
function getRiskLevel(score: number): ThreatIntelligence['riskLevel']
function generateThreatSummary(factors: RiskFactors, cves: CVEData[], advisories: GitHubAdvisory[]): string
function generateRecommendations(factors: RiskFactors, cves: CVEData[], advisories: GitHubAdvisory[]): string[]
function isVersionAffected(currentVersion: string, advisory: GitHubAdvisory): boolean
function findSafeVersion(advisories: GitHubAdvisory[], currentVersion: string): string | undefined
```

From types.ts:
```
interface GitHubAdvisory { id: string; summary: string; description: string; severity: 'LOW'|'MODERATE'|'HIGH'|'CRITICAL'; publishedAt: string; updatedAt: string; withdrawnAt?: string; vulnerabilities: { package: { name: string; ecosystem: string }; vulnerableVersionRange: string; firstPatchedVersion?: string }[]; references: { url: string }[]; cvss?: { score: number; vectorString: string } }
interface RiskFactors { dependencyAge: number; knownExploits: number; criticalCVEs: number; highCVEs: number; outdatedDependencies: number; transitiveVulnerabilities: number }
```

Existing MSW pattern (from fix-generator.test.ts):
```
import { server } from '../../../vitest.setup';
import { http, HttpResponse } from 'msw';
server.use(http.get('https://api.github.com/advisories', () => { return HttpResponse.json([...]); }));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test github-advisories — GitHub API requests with MSW</name>
  <files>src/lib/threat-intel/github-advisories.test.ts</files>
  <behavior>
    - fetchReactAdvisories makes request to GitHub API and returns GitHubAdvisory[]
    - GitHub API error (403, 500) returns empty array
    - fetchAdvisoriesForPackage returns advisories for a specific package
    - searchAdvisoriesByKeyword filters advisories by keyword in summary/description
    - Deduplication by advisory ID across multiple package queries
  </behavior>
  <action>
    Create test file at src/lib/threat-intel/github-advisories.test.ts.

    Mock strategy:
    - MSW intercepts GET to `https://api.github.com/advisories`
    - Mock the memory-cache module: `vi.mock('../threat-intel/memory-cache', () => ({ getCachedThreatData: vi.fn().mockResolvedValue(null), setCachedThreatData: vi.fn().mockResolvedValue(undefined) }))`
    - Note: github-advisories imports from `'./memory-cache'` (same dir), so mock path should be `'./memory-cache'`

    Mock GitHub Advisory API response:
    ```
    [{ ghsa_id: 'GHSA-xxxx-xxxx-xxxx', summary: 'React XSS vulnerability', description: 'A cross-site scripting vulnerability...', severity: 'HIGH', published_at: '2024-01-15T00:00:00Z', updated_at: '2024-06-01T00:00:00Z', vulnerabilities: [{ package: { name: 'react', ecosystem: 'npm' }, vulnerable_version_range: '>=16.0.0 <18.2.0', first_patched_version: { identifier: '18.2.0' } }], references: [{ url: 'https://github.com/advisories/GHSA-xxxx' }], cvss: { score: 7.5, vector_string: 'CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N' } }]
    ```

    Test scenarios:
    1. fetchReactAdvisories success: MSW intercepts with 2 advisories. fetchReactAdvisories(5) returns array of 2 GitHubAdvisory objects with id, summary, severity populated.
    2. Error handling: MSW returns 403. fetchReactAdvisories(5) returns empty array.
    3. fetchAdvisoriesForPackage: MSW intercepts with advisories containing 'react'. fetchAdvisoriesForPackage('react') returns GitHubAdvisory[] with correct package name.
    4. searchAdvisoriesByKeyword: MSW returns advisories with various summary texts. Search for 'XSS' returns only those with 'XSS' in summary/description.
    5. Deduplication: Mock identical advisory IDs across multiple package queries. fetchReactAdvisories should dedup by id.
    6. parseAdvisory: After successful fetch, verify advisory.severity is upper-cased, advisory.references is array of {url}, advisory.cvss has score and vectorString.
  </action>
  <verify>
    <automated>npx vitest run src/lib/threat-intel/github-advisories.test.ts --reporter=verbose 2>&1 | head -50</automated>
  </verify>
  <done>github-advisories tests pass. MSW mocks GitHub API. Response parsing converts to GitHubAdvisory type. Error handling returns empty array. Dedup works.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test risk-analyzer internal functions (pure logic)</name>
  <files>src/lib/threat-intel/risk-analyzer.test.ts</files>
  <behavior>
    - calculatePackageRiskScore: CRITICAL CVE adds 25pts, HIGH adds 15pts, MEDIUM adds 8pts, LOW adds 3pts, exploitAvailable adds 10pts bonus
    - calculatePackageRiskScore: advisory severity scoring with isVersionAffected check
    - calculateRiskFactors: counts criticalCVEs, highCVEs, knownExploits, outdatedDependencies correctly
    - calculateOverallRiskScore: criticalCVEs * 35 + highCVEs * 20 + knownExploits * 25 + advisoryScore (logarithmic) + transitiveVulns
    - getRiskLevel: >75=CRITICAL, >50=HIGH, >25=MEDIUM, else LOW
    - generateThreatSummary: includes counts for critical, exploits, CVEs, advisories
    - generateRecommendations: includes specific messages for different factor thresholds
    - isVersionAffected: wildcard '*' returns true, range containing version returns true
    - findSafeVersion: returns firstPatchedVersion from first advisory with one
  </behavior>
  <action>
    Create test file at src/lib/threat-intel/risk-analyzer.test.ts.

    These are PURE LOGIC tests — no HTTP mocking needed. Mock the imported dependencies:
    ```
    vi.mock('./cve-fetcher', () => ({ fetchCVEsByPackage: vi.fn() }));
    vi.mock('./github-advisories', () => ({ fetchAdvisoriesForPackage: vi.fn() }));
    ```

    Define CVEData fixtures inline:
    ```
    const mockCriticalCVE: CVEData = { id: 'CVE-2024-0001', description: 'Critical vuln', severity: 'CRITICAL', cvssScore: 9.0, publishedDate: '2024-01-01', lastModifiedDate: '2024-06-01', affectedPackages: ['react'], references: [], exploitAvailable: true };
    const mockHighCVE: CVEData = { id: 'CVE-2024-0002', description: 'High vuln', severity: 'HIGH', cvssScore: 7.5, publishedDate: '2024-01-01', lastModifiedDate: '2024-06-01', affectedPackages: ['react'], references: [] };
    const mockMediumCVE: CVEData = { id: 'CVE-2024-0003', description: 'Medium vuln', severity: 'MEDIUM', cvssScore: 5.0, publishedDate: '2024-01-01', lastModifiedDate: '2024-06-01', affectedPackages: ['react'], references: [] };
    ```

    Test scenarios:
    1. calculatePackageRiskScore with no CVEs/advisories returns 0
    2. calculatePackageRiskScore with 1 critical CVE (exploit available) returns 35 (25+10)
    3. calculatePackageRiskScore with 1 HIGH CVE (no exploit) returns 15
    4. calculatePackageRiskScore with 1 CRITICAL advisory (version affected) returns 20
    5. calculatePackageRiskScore caps at 100
    6. calculateRiskFactors counts correctly from CVE/advisory arrays
    7. calculateOverallRiskScore: 1 critical CVE + 1 exploit = (1*35)+(1*25)=60, before advisory/transitive additions
    8. getRiskLevel: 0 returns 'LOW', 25 returns 'MEDIUM', 50 returns 'HIGH', 75 returns 'CRITICAL'
    9. generateThreatSummary: with criticalCVEs=2 returns '2 critical vulnerabilities detected'
    10. generateThreatSummary: with no data returns 'No significant threats detected'
    11. generateRecommendations: with criticalCVEs>0 includes 'Immediate action required' message
    12. generateRecommendations: with no data returns monitoring message
    13. isVersionAffected: '*' returns true, '>=16.0.0 <18.2.0' with version '17.0.0' returns true (range includes version)
    14. findSafeVersion: returns firstPatchedVersion from first advisory's first vulnerability
  </action>
  <verify>
    <automated>npx vitest run src/lib/threat-intel/risk-analyzer.test.ts --reporter=verbose 2>&1 | head -80</automated>
  </verify>
  <done>Risk-analyzer internal function tests pass. All scoring, risk level, summary, recommendation, version check, safe version functions verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test risk-analyzer exported functions (analyzePackageRisk, generateThreatIntelligence)</name>
  <files>src/lib/threat-intel/risk-analyzer.test.ts</files>
  <behavior>
    - analyzePackageRisk calls fetchCVEsByPackage and fetchAdvisoriesForPackage then calculates risk score
    - generateThreatIntelligence iterates all dependencies and aggregates results
  </behavior>
  <action>
    Add tests to the same file for the exported functions:

    These functions call cve-fetcher and github-advisories, which are mocked:
    ```
    import * as cveFetcher from './cve-fetcher';
    import * as githubAdvisories from './github-advisories';
    // Use vi.mocked() to set return values
    vi.mocked(cveFetcher.fetchCVEsByPackage).mockResolvedValue([mockCriticalCVE]);
    vi.mocked(githubAdvisories.fetchAdvisoriesForPackage).mockResolvedValue([]);
    ```

    Test scenarios:
    1. analyzePackageRisk('react', '18.0.0'): Mock fetchCVEsByPackage to return [mockCriticalCVE] and fetchAdvisoriesForPackage to return []. Call analyzePackageRisk. Verify: packageName='react', currentVersion='18.0.0', vulnerabilities=[mockCriticalCVE], advisories=[], riskScore > 0.
    2. generateThreatIntelligence({ 'react': '18.0.0', 'next': '14.0.0' }): Mock both fetchers to return CVEs/advisories. Call generateThreatIntelligence. Verify: cveMatches includes CVEs from both packages, riskScore is computed, riskLevel is set, threatSummary and recommendations are non-empty arrays/strings, lastUpdated is an ISO string.
    3. Empty dependencies: generateThreatIntelligence({}) returns ThreatIntelligence with empty cveMatches, empty advisoryMatches, riskScore=0, riskLevel='LOW'.

    Use `beforeEach` with `vi.clearAllMocks()` to reset mock state between tests.
  </action>
  <verify>
    <automated>npx vitest run src/lib/threat-intel/risk-analyzer.test.ts --reporter=verbose 2>&1</automated>
  </verify>
  <done>Risk-analyzer exported function tests pass. analyzePackageRisk produces PackageVulnerability with correct structure. generateThreatIntelligence aggregates across dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test -> MSW HTTP mock | No real HTTP — MSW intercepts GitHub API calls |
| test -> mocked imports | cve-fetcher and github-advisories mocked for risk-analyzer tests |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Spoofing | MSW handler URL mismatch | mitigate | Test URL patterns match actual GitHub API endpoints used by github-advisories |
| T-04-04-02 | Tampering | Mock leakage between tests | mitigate | vi.clearAllMocks() in beforeEach ensures clean mock state |
</threat_model>

<verification>
```
npx vitest run src/lib/threat-intel/github-advisories.test.ts src/lib/threat-intel/risk-analyzer.test.ts --reporter=verbose
```
Expected: All tests pass, MSW mocks GitHub API, risk scoring math verified.
</verification>

<success_criteria>
- github-advisories.test.ts tests all 3 exported functions with MSW
- risk-analyzer.test.ts tests all 8 internal functions directly + 2 exported functions
- Risk scoring calculations verified mathematically
- All tests pass under vitest run
</success_criteria>
