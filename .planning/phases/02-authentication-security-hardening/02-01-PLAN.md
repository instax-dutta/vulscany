---
phase: 02-authentication-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/proxy.ts
  - src/app/api/ai/batch-fix/route.ts
  - src/app/api/threat-intel/route.ts
autonomous: true
requirements: [REQ-6]

must_haves:
  truths:
    - "Unauthenticated requests to /api/* (excluding /api/auth/*) are rejected at the proxy layer with 401 JSON"
    - "Unauthenticated requests to /dashboard are still redirected to /"
    - "Authenticated requests to /api/* pass through normally"
    - "API key rotation uses round-robin with failure penalty (verified, no change needed)"
  artifacts:
    - path: "src/proxy.ts"
      provides: "API auth guard returning JSON 401 for /api/* routes"
      exists: true
      contains:
        - "auth check for /api/:path* with JSON 401 response"
        - "exclusion for /api/auth/:path*"
    - path: "src/app/api/ai/batch-fix/route.ts"
      provides: "Auth check in route handler (defense-in-depth)"
      min_lines: 25
      contains: "cookies"
    - path: "src/app/api/threat-intel/route.ts"
      provides: "Auth check in route handler (defense-in-depth)"
      min_lines: 47
      contains: "cookies"
  key_links:
    - from: "src/proxy.ts"
      to: "/api/*"
      via: "request.nextUrl.pathname check"
      pattern: "pathname\\.startsWith\\('/api/'"
    - from: "src/proxy.ts"
      to: "NextResponse.json.*401"
      via: "401 response for API routes"
      pattern: "status: 401"
---

<objective>
Standardize API authentication — proxy.ts now guards all API routes consistently with the same pattern used for /dashboard, and two routes that were missing auth checks get defense-in-depth route-level guards.

Purpose: Close the auth gap where API routes had inconsistent auth protection. Middleware/proxy now protects both `/dashboard` (page routes) and `/api/*` (API routes) uniformly. Two unguarded API routes (batch-fix, threat-intel) get route-level auth for defense-in-depth.

Output:
- Updated `proxy.ts` with `/api/*` auth guard returning JSON 401
- Updated `batch-fix/route.ts` and `threat-intel/route.ts` with auth checks
- Verified `rotation.ts` round-robin implementation is already correct
</objective>

<execution_context>
@/Users/saiduttaabhishekdash/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/saiduttaabhishekdash/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication-security-hardening

<interfaces>
From src/proxy.ts (existing):
```
export function proxy(request: NextRequest) {
  // Line 40: current auth check for /dashboard
  if (request.nextUrl.pathname.startsWith('/dashboard')) {
    const token = request.cookies.get('github_token')
    if (!token) {
      return NextResponse.redirect(new URL('/', request.url))
    }
  }
}
```

From src/app/api/auth/callback/route.ts (existing auth pattern):
```
import { cookies } from 'next/headers';
const cookieStore = await cookies();
const token = cookieStore.get('github_token')?.value;
if (!token) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add API auth guard to proxy.ts</name>
  <files>
    src/proxy.ts
  </files>
  <read_first>
    src/proxy.ts
  </read_first>
  <action>
    In `src/proxy.ts`, add an auth guard block for `/api/*` routes AFTER the existing `/dashboard` auth check (after line 45, before `return response` on line 47). The new block must:

    1. Check `if (request.nextUrl.pathname.startsWith('/api/') && !request.nextUrl.pathname.startsWith('/api/auth/'))`
       - `/api/auth/*` routes must be excluded (OAuth callback, session, logout are public entry points)
    2. Get token via `request.cookies.get('github_token')`
    3. If no token: return `NextResponse.json({ error: 'Unauthorized' }, { status: 401 })`
       - API routes MUST get JSON 401, NOT redirect (which would break API consumers)
    4. Keep the existing `/dashboard` redirect auth check unchanged (302 redirect is correct for pages)
    5. Keep all CSP, nonce, header logic unchanged

    Do NOT remove the existing `/dashboard` auth check — that handles page routes with redirect. The new `/api/` block handles API routes with JSON response. Both blocks read the same `github_token` cookie.

    After auth check logic, verify `rotation.ts` in `src/lib/ai/rotation.ts` already implements round-robin with failure penalty. The `getNextKey()` method at line 46 uses `(this.currentIndex + 1) % availableKeys.length`. The `markKeyFailed()` at line 50 tracks failures and applies cooldown. The `maxFailures` threshold and `cooldownPeriod` are already configured. No code change needed — just verify and note it in the summary.
  </action>
  <verify>
    <automated>
      grep -n "pathname.startsWith('/api/" src/proxy.ts | wc -l | tr -d ' ' | grep -q '^1$' || (echo "FAIL: API auth check not found" && false)
      grep -n "pathname.startsWith('/api/auth/'" src/proxy.ts | wc -l | tr -d ' ' | grep -q '^1$' || (echo "FAIL: Auth exclusion not found" && false)
      grep -c "status: 401" src/proxy.ts | grep -q '1' || (echo "FAIL: 401 response not found" && false)
      grep -c "status: 401" src/proxy.ts | grep -q '1' || (echo "FAIL: 401 response not found" && false)
      npx tsc --noEmit --noErrorTruncation 2>&1 | head -20 | grep -q 'error' && (echo "FAIL: TypeScript errors" && false) || true
    </automated>
  </verify>
  <acceptance_criteria>
    - proxy.ts has `/api/` path check that excludes `/api/auth/`
    - proxy.ts returns JSON `{ error: 'Unauthorized' }` with status 401 for unauthenticated API requests
    - proxy.ts still redirects `/dashboard` to `/` for unauthenticated page requests (unchanged)
    - `npx tsc --noEmit` passes (0 errors)
    - rotation.ts confirmed to use round-robin (index + 1) with failure penalty tracking
  </acceptance_criteria>
  <done>
    proxy.ts guards both `/dashboard` (redirect) and `/api/*` (JSON 401) consistently. rotation.ts verified as round-robin. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add defense-in-depth auth checks to unguarded API routes</name>
  <files>
    src/app/api/ai/batch-fix/route.ts
    src/app/api/threat-intel/route.ts
  </files>
  <read_first>
    src/app/api/ai/batch-fix/route.ts
    src/app/api/threat-intel/route.ts
  </read_first>
  <action>
    Two API routes have no auth check at the route level. Even though proxy.ts now guards `/api/*`, these routes should have their own auth check for defense-in-depth (matching the pattern used by all other API routes).

    **batch-fix/route.ts:**
    - Add `import { cookies } from 'next/headers'` at top (below existing imports)
    - At the start of the `POST` function body (before `const { repoName... } = await request.json()`), add:
      ```
      const cookieStore = await cookies();
      const token = cookieStore.get('github_token')?.value;
      if (!token) {
          return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
      }
      ```
    - The `request` parameter is already `Request` type. Change to `NextRequest` to get `cookies()` compatibility, or keep `Request` since `cookies()` is imported separately.

    **threat-intel/route.ts:**
    - Add `import { cookies } from 'next/headers'` at top
    - In the `GET` handler: add auth check before the `switch(action)` block
    - In the `POST` handler: add auth check before `const body = await request.json();`
    - Use the same pattern: `cookieStore.get('github_token')?.value` → 401 if missing

    For BOTH routes, the cookie check should come BEFORE any business logic or JSON parsing to minimize attack surface.
  </action>
  <verify>
    <automated>
      grep -c "import { cookies }" src/app/api/ai/batch-fix/route.ts | grep -q '1' && echo "PASS: batch-fix cookies import"
      grep -c "import { cookies }" src/app/api/threat-intel/route.ts | grep -q '1' && echo "PASS: threat-intel cookies import"
      grep -c "status: 401" src/app/api/ai/batch-fix/route.ts | grep -q '1' && echo "PASS: batch-fix 401"
      grep -c "status: 401" src/app/api/threat-intel/route.ts | grep -q '2' && echo "PASS: threat-intel 401 (GET + POST)"
      npx vitest run --reporter=verbose 2>&1 | tail -30 | grep -E "Tests|FAIL|PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - batch-fix/route.ts has `cookies()` import and `github_token` auth check returning 401
    - threat-intel/route.ts has `cookies()` import and `github_token` auth check in both GET and POST handlers
    - All 47+ existing tests still pass
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>
    Both batch-fix and threat-intel routes now have defense-in-depth auth checks. All tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → proxy | Untrusted HTTP request enters the application at the proxy layer. Proxy now enforces auth for both page routes and API routes. |
| proxy → API route | Authenticated-only path after proxy auth check. Defense-in-depth checks in route handlers for the two previously-unguarded routes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | proxy.ts /api/* auth guard | mitigate | Check `github_token` cookie at proxy layer before request reaches route handler. Return JSON 401 with no additional info. |
| T-02-02 | Spoofing | batch-fix/route.ts, threat-intel/route.ts | mitigate | Add defense-in-depth route-level `github_token` check matching the established pattern across all other API routes. |
| T-02-SC | Tampering | npm installs | mitigate | No new packages added. Existing packages remain. slopcheck not triggered. |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes (0 errors)
2. `npx vitest run` passes (all tests)
3. proxy.ts has both `/dashboard` redirect auth and `/api/*` JSON 401 auth
4. batch-fix and threat-intel routes have defense-in-depth auth checks
5. rotation.ts confirmed to use round-robin (no code change)
</verification>

<success_criteria>
- Unauthenticated GET /api/scan returns 401 JSON (not redirect)
- Unauthenticated GET /api/threat-intel returns 401 JSON
- Unauthenticated POST /api/ai/batch-fix returns 401 JSON
- Unauthenticated GET /dashboard still redirects to /
- Authenticated requests pass through normally
- All existing tests pass
- TypeScript compiles cleanly
</success_criteria>

<output>
Create `.planning/phases/02-authentication-security-hardening/02-01-SUMMARY.md` when done.
</output>
