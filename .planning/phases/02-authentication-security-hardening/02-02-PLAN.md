---
phase: 02-authentication-security-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/threat-intel/index.ts
autonomous: true
requirements: [REQ-9, REQ-7]

must_haves:
  truths:
    - "hashDependencies uses crypto.createHash('sha256') instead of custom hash algorithm"
    - "Cache keys are collision-resistant (SHA-256, not custom additive hash)"
    - "Session cookies (github_token, session) have Secure and SameSite=Lax attributes"
  artifacts:
    - path: "src/lib/threat-intel/index.ts"
      provides: "hashDependencies function using crypto.createHash('sha256')"
      contains:
        - "crypto.createHash('sha256')"
        - "digest('hex')"
    - path: "src/app/api/auth/callback/route.ts"
      provides: "Cookie options with Secure and SameSite=Lax"
      contains:
        - "secure:"
        - "sameSite: 'lax'"
    - path: "src/app/api/auth/logout/route.ts"
      provides: "Cookie options with Secure and SameSite=Lax"
      contains:
        - "secure:"
        - "sameSite: 'lax'"
  key_links:
    - from: "src/lib/threat-intel/index.ts"
      to: "crypto.createHash"
      via: "hashDependencies function body"
      pattern: "crypto\\.createHash"
---

<objective>
Replace the collision-prone custom hash in hashDependencies with standard crypto.createHash('sha256'), and verify existing cookie security attributes.

Purpose: The custom hashDependencies function (lines 121-131 of threat-intel/index.ts) uses a basic additive hash that can produce collisions. Replacing with SHA-256 eliminates collision risk for cache keys. Separately, REQ-7 (Secure; SameSite=Lax on session cookies) is already implemented in callback and logout routes — we verify and document.

Output:
- Updated `hashDependencies` in `src/lib/threat-intel/index.ts` using `crypto.createHash('sha256')`
- Verified cookie options in `callback/route.ts` and `logout/route.ts`
</objective>

<execution_context>
@/Users/saiduttaabhishekdash/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/saiduttaabhishekdash/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication-security-hardening

<interfaces>
From src/lib/threat-intel/index.ts (current hashDependencies, lines 121-131):
```typescript
function hashDependencies(deps: Record<string, string>): string {
    const sorted = Object.keys(deps).sort().map(k => `${k}@${deps[k]}`).join(',');
    // Simple hash for cache key
    let hash = 0;
    for (let i = 0; i < sorted.length; i++) {
        const char = sorted.charCodeAt(i);
        hash = ((hash << 5) - hash) + char;
        hash = hash & hash;
    }
    return Math.abs(hash).toString(36);
}
```

From src/app/api/auth/callback/route.ts (current cookie options, lines 61-67):
```typescript
const cookieOptions = {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'lax' as const,
    maxAge: 60 * 60 * 2,
    path: '/',
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hashDependencies with crypto.createHash('sha256')</name>
  <files>
    src/lib/threat-intel/index.ts
  </files>
  <read_first>
    src/lib/threat-intel/index.ts
  </read_first>
  <action>
    In `src/lib/threat-intel/index.ts`, replace the private `hashDependencies` function (lines 121-131) with a SHA-256-based implementation using Node.js's built-in `crypto` module.

    Requirements:
    1. Do NOT add an import statement — `crypto` is a global Node.js module available without import in Next.js server runtime.
    2. The function signature MUST remain identical: `function hashDependencies(deps: Record<string, string>): string`
    3. The string preparation logic (sorting keys, joining as `key@value`) MUST remain identical — only the hashing algorithm changes
    4. Replace the custom loop-based hash with:
       ```typescript
       const hash = crypto.createHash('sha256').update(sorted).digest('hex');
       return hash.substring(0, 12);
       ```
       - The 12-character prefix preserves a readable cache key length while providing 48 bits of collision resistance
       - Use `substring` not `substr` (substr is legacy)
    5. Verify the function is used only in one place: `const cacheKey = \`threats:repo:${hashDependencies(dependencies)}\`;` at line 51 (unchanged)
    6. Run `npx tsc --noEmit` and `npx vitest run` to verify nothing broke

    The sorted string format stays as `"key1@value1,key2@value2"` — the same input, now hashed with SHA-256 instead of the custom additive algorithm.
  </action>
  <verify>
    <automated>
      grep -c "crypto.createHash" src/lib/threat-intel/index.ts | grep -q '1' || (echo "FAIL: crypto.createHash not found" && false)
      grep -c "digest" src/lib/threat-intel/index.ts | grep -q '1' || (echo "FAIL: digest not found" && false)
      grep -c "hashDependencies" src/lib/threat-intel/index.ts | grep -q '2' || (echo "FAIL: Expected 2 references (definition + usage)" && false)
      # Verify old hash code is gone
      grep -c "let hash = 0" src/lib/threat-intel/index.ts | grep -q '0' || (echo "FAIL: Old hash code still present" && false)
      grep -c "hash << 5" src/lib/threat-intel/index.ts | grep -q '0' || (echo "FAIL: Old bit-shift code still present" && false)
      npx tsc --noEmit 2>&1 | head -20 | grep -q 'error' && (echo "FAIL: TypeScript errors" && false) || echo "PASS: No TS errors"
      npx vitest run --reporter=verbose 2>&1 | tail -10 | grep -E "Tests|FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - hashDependencies no longer uses the custom additive hash algorithm
    - hashDependencies uses `crypto.createHash('sha256').update(sorted).digest('hex')` with 12-char prefix
    - All existing callers unchanged (only the function body changes)
    - TypeScript compiles cleanly
    - All existing tests pass
  </acceptance_criteria>
  <done>
    hashDependencies produces SHA-256-based cache keys. Old custom hash removed. Tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify cookie security attributes (REQ-7)</name>
  <files>
    src/app/api/auth/callback/route.ts
    src/app/api/auth/logout/route.ts
  </files>
  <read_first>
    src/app/api/auth/callback/route.ts
    src/app/api/auth/logout/route.ts
  </read_first>
  <action>
    REQ-7 requires `Secure; SameSite=Lax` on session cookies. Verify both cookie-setting routes already have these attributes. No code change is expected — this is a verification-only task.

    Check in `callback/route.ts`:
    - `cookieOptions` (lines 61-67) should have:
      - `secure: process.env.NODE_ENV === 'production'` (correct — conditional on HTTPS)
      - `sameSite: 'lax' as const`
      - `httpOnly: true`
    - Cookies set at lines 69 and 77 use `cookieOptions`

    Check in `logout/route.ts`:
    - `cookieOptions` (lines 11-17) should have:
      - `secure: process.env.NODE_ENV === 'production'`
      - `sameSite: 'lax' as const`
      - `httpOnly: true`
    - Cookies set at lines 19 and 20 use `cookieOptions`

    If all attributes are present, document in the plan summary that REQ-7 is already satisfied (no code change needed).

    If any attribute is missing, add it to match the pattern: secure conditional on production, sameSite: 'lax', httpOnly: true.
  </action>
  <verify>
    <automated>
      grep -c "secure:" src/app/api/auth/callback/route.ts | grep -q '1' || (echo "FAIL: callback missing secure" && false)
      grep -c "sameSite:" src/app/api/auth/callback/route.ts | grep -q '1' || (echo "FAIL: callback missing sameSite" && false)
      grep -c "secure:" src/app/api/auth/logout/route.ts | grep -q '1' || (echo "FAIL: logout missing secure" && false)
      grep -c "sameSite:" src/app/api/auth/logout/route.ts | grep -q '1' || (echo "FAIL: logout missing sameSite" && false)
      grep -c "httpOnly: true" src/app/api/auth/callback/route.ts | grep -q '1' || (echo "FAIL: callback missing httpOnly" && false)
      grep -c "httpOnly: true" src/app/api/auth/logout/route.ts | grep -q '1' || (echo "FAIL: logout missing httpOnly" && false)
      echo "PASS: All cookie security attributes verified"
    </automated>
  </verify>
  <acceptance_criteria>
    - callback/route.ts cookieOptions has `secure: process.env.NODE_ENV === 'production'`, `sameSite: 'lax'`, `httpOnly: true`
    - logout/route.ts cookieOptions has `secure: process.env.NODE_ENV === 'production'`, `sameSite: 'lax'`, `httpOnly: true`
    - No regressions — existing tests pass
  </acceptance_criteria>
  <done>
    REQ-7 cookie security attributes verified. No code change needed — both routes already have Secure (conditional) + SameSite=Lax + httpOnly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → threat-intel cache | Cache key generated from user dependency data. SHA-256 eliminates collision risk. |
| client → auth cookie | Cookie transmitted with Secure (HTTPS-only in production), SameSite=Lax (prevents CSRF from external sites), httpOnly (prevents XSS access). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | hashDependencies (threat-intel/index.ts) | mitigate | Replace collision-prone custom additive hash with SHA-256 via crypto.createHash for deterministic, collision-resistant cache keys. |
| T-02-04 | Information Disclosure | session cookies | mitigate | Cookies have httpOnly (prevents JavaScript access), Secure (HTTPS-only in production), SameSite=Lax (CSRF protection). All three attributes verified present. |
| T-02-SC | Tampering | npm installs | mitigate | No new packages added. crypto is a built-in Node.js module. slopcheck not triggered. |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes (0 errors)
2. `npx vitest run` passes (all tests)
3. grep confirms SHA-256 usage in hashDependencies
4. grep confirms Secure, SameSite=Lax, httpOnly on both cookie-setting routes
</verification>

<success_criteria>
- hashDependencies no longer uses custom additive algorithm
- SHA-256 used for cache key hashing
- Cookie security attributes verified present and correct
- TypeScript compiles cleanly
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/02-authentication-security-hardening/02-02-SUMMARY.md` when done.
</output>
