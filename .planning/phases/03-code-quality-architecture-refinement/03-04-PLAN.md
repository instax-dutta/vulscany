---
phase: 03-code-quality-architecture-refinement
plan: 04
type: execute
wave: 2
depends_on: [03-03]
files_modified:
  - src/components/DashboardFeatures.tsx (deleted)
  - src/components/DashboardFeatures/index.ts (created)
  - src/components/DashboardFeatures/SecurityScoreWidget.tsx (created)
  - src/components/DashboardFeatures/AchievementsPanel.tsx (created)
  - src/components/DashboardFeatures/EducationPanel.tsx (created)
  - src/components/DashboardFeatures/CommunityPatternsPanel.tsx (created)
  - src/components/DashboardFeatures/SecurityTipBanner.tsx (created)
autonomous: true
requirements: [REQ-11]
user_setup: []
must_haves:
  truths:
    - "Each DashboardFeature component exists as its own file with proper exports"
    - "Barrel export (index.ts) re-exports all 5 components with same names"
    - "Import statements elsewhere (page.tsx) continue to work without changes"
    - "TypeScript compiles without errors"
    - "All existing tests still pass"
  artifacts:
    - path: "src/components/DashboardFeatures/index.ts"
      provides: "Barrel export for backward-compatible importing"
      contains: "export"
    - path: "src/components/DashboardFeatures/SecurityScoreWidget.tsx"
      provides: "SecurityScoreWidget component"
      exports: ["SecurityScoreWidget"]
    - path: "src/components/DashboardFeatures/AchievementsPanel.tsx"
      provides: "AchievementsPanel component"
      exports: ["AchievementsPanel"]
    - path: "src/components/DashboardFeatures/EducationPanel.tsx"
      provides: "EducationPanel component"
      exports: ["EducationPanel"]
    - path: "src/components/DashboardFeatures/CommunityPatternsPanel.tsx"
      provides: "CommunityPatternsPanel component"
      exports: ["CommunityPatternsPanel"]
    - path: "src/components/DashboardFeatures/SecurityTipBanner.tsx"
      provides: "SecurityTipBanner component"
      exports: ["SecurityTipBanner"]
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "@/components/DashboardFeatures"
      via: "import { ... } from"
      pattern: "from '@/components/DashboardFeatures'"
---

<objective>
Split the monolithic `DashboardFeatures.tsx` (834 lines, 5 exported components) into focused, individually importable modules under `src/components/DashboardFeatures/`.

**Purpose:** Isolate each component's lifecycle, reduce re-render scope, improve code navigation, and enable tree-shaking for components not used on every render.

**Output:**
- `src/components/DashboardFeatures.tsx` removed
- `src/components/DashboardFeatures/` directory with `index.ts` barrel and 5 component files
</objective>

<execution_context>
@/Users/saiduttaabhishekdash/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/saiduttaabhishekdash/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/saiduttaabhishekdash/vulscany/.planning/ROADMAP.md
@/Users/saiduttaabhishekdash/vulscany/.planning/STATE.md

@/Users/saiduttaabhishekdash/vulscany/src/components/DashboardFeatures.tsx (834 lines — source to split)
@/Users/saiduttaabhishekdash/vulscany/src/app/dashboard/page.tsx (consumer with imports)

<interfaces>
Current import in page.tsx for reference:
```typescript
import {
    SecurityScoreWidget,
    AchievementsPanel,
    EducationPanel,
    CommunityPatternsPanel,
    SecurityTipBanner
} from '@/components/DashboardFeatures';
```

Each exported component from DashboardFeatures.tsx:

1. `SecurityScoreWidget({ vulnerabilities, threatIntel, onScoreCalculated? })`
   - Internal: `score`, `animatedScore` useState, `useEffect` for calculation + animation

2. `AchievementsPanel({ stats }: UserStats)`
   - Internal: `expanded` useState, uses `getEarnedAchievements`, `getInProgressAchievements`

3. `EducationPanel({ vulnerabilityType, isSimpleMode? })`
   - Internal: `content`, `mode`, `expanded` useState, uses `getEducation`

4. `CommunityPatternsPanel({ onPatternSubmit? })`
   - Internal: `patterns`, `showSubmit`, `formData` useState, uses `loadCommunityPatterns`, `submitPattern`, `voteForPattern`, `hasVotedFor`, `getTopPatterns`

5. `SecurityTipBanner()`
   - Internal: `tip` useState, uses `getRandomTip`

All imports from `@/lib/security-score`, `@/lib/education`, `@/lib/community-patterns` are shared.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create individual component files for each DashboardFeature</name>
  <files>
    src/components/DashboardFeatures/SecurityScoreWidget.tsx (create)
    src/components/DashboardFeatures/AchievementsPanel.tsx (create)
    src/components/DashboardFeatures/EducationPanel.tsx (create)
    src/components/DashboardFeatures/CommunityPatternsPanel.tsx (create)
    src/components/DashboardFeatures/SecurityTipBanner.tsx (create)
  </files>
  <read_first>
    src/components/DashboardFeatures.tsx — read the full file. Sections:
    - SecurityScoreWidget: lines 47-151
    - AchievementsPanel: lines 159-293
    - EducationPanel: lines 302-494
    - CommunityPatternsPanel: lines 502-804
    - SecurityTipBanner: lines 806-834

    Note all shared imports from:
    - `@/lib/security-score` (calculateScore, getScoreColor, getScoreLabel, loadUserStats, saveUserStats, updateStatsAfterScan, getEarnedAchievements, getInProgressAchievements, UserStats, SecurityScore, Achievement)
    - `@/lib/education` (getEducation, getRandomTip, EducationalContent)
    - `@/lib/community-patterns` (loadCommunityPatterns, submitPattern, voteForPattern, hasVotedFor, getTopPatterns, CATEGORY_LABELS, SEVERITY_COLORS, CommunityPattern)
  </read_first>
  <action>
    Create five component files under `src/components/DashboardFeatures/`. Each file gets:
    - `'use client'` directive (matching the original)
    - Only the imports it actually uses (not all the original imports)
    - Its component function (exact same code, not modified)
    - Named export matching the original export name

    **File mappings (exact code extraction):**

    1. **`SecurityScoreWidget.tsx`** — Lines 47-151 from original:
       - Import `{ useState, useEffect } from 'react'`
       - Import `{ motion } from 'framer-motion'`
       - Import `{ calculateScore, getScoreColor, getScoreLabel, type SecurityScore } from '@/lib/security-score'`
       - Export `SecurityScoreWidget`

    2. **`AchievementsPanel.tsx`** — Lines 159-293 from original:
       - Import `{ useState } from 'react'`
       - Import `{ motion, AnimatePresence } from 'framer-motion'`
       - Import `{ getEarnedAchievements, getInProgressAchievements, type UserStats } from '@/lib/security-score'`
       - Export `AchievementsPanel`

    3. **`EducationPanel.tsx`** — Lines 302-494 from original:
       - Import `{ useState, useEffect } from 'react'`
       - Import `{ motion, AnimatePresence } from 'framer-motion'`
       - Import `{ getEducation, type EducationalContent } from '@/lib/education'`
       - Export `EducationPanel`

    4. **`CommunityPatternsPanel.tsx`** — Lines 502-804 from original:
       - Import `{ useState, useEffect } from 'react'`
       - Import `{ motion, AnimatePresence } from 'framer-motion'`
       - Import `{ loadCommunityPatterns, submitPattern, voteForPattern, hasVotedFor, getTopPatterns, CATEGORY_LABELS, SEVERITY_COLORS, type CommunityPattern } from '@/lib/community-patterns'`
       - Export `CommunityPatternsPanel`

    5. **`SecurityTipBanner.tsx`** — Lines 806-834 from original:
       - Import `{ useState, useEffect } from 'react'`
       - Import `{ motion } from 'framer-motion'`
       - Import `{ getRandomTip } from '@/lib/education'`
       - Export `SecurityTipBanner`

    **Critical rules:**
    - Do NOT change any component logic, props, or behavior
    - Do NOT rename exports
    - Preserve exactly the same JSX, state hooks, effects, and event handlers
    - Each file must compile independently: `npx tsc --noEmit`
  </action>
  <verify>
    <automated>ls -la src/components/DashboardFeatures/*.tsx | wc -l | grep -q 5 && echo "5 component files created"</automated>
    <automated>grep -n "export function\|export default" src/components/DashboardFeatures/*.tsx | grep -E "SecurityScoreWidget|AchievementsPanel|EducationPanel|CommunityPatternsPanel|SecurityTipBanner" | wc -l | grep -q 5 && echo "All 5 exports present"</automated>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -10    <automated>npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 5 component files created in src/components/DashboardFeatures/
    - Each file contains exactly one named export (same component name as original)
    - Each file includes only the imports it actually uses
    - Component logic is identical to original DashboardFeatures.tsx
    - TypeScript compiles without errors
    - All existing tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create barrel export and remove monolithic file</name>
  <files>
    src/components/DashboardFeatures/index.ts (create)
    src/components/DashboardFeatures.tsx (delete)
  </files>
  <read_first>
    src/components/DashboardFeatures.tsx — confirm all 5 named exports
    src/app/dashboard/page.tsx — confirm the exact import path used (@/components/DashboardFeatures)
  </read_first>
  <action>
    1. **Create `src/components/DashboardFeatures/index.ts`** — a barrel export that re-exports all 5 components:
       ```typescript
       export { SecurityScoreWidget } from './SecurityScoreWidget';
       export { AchievementsPanel } from './AchievementsPanel';
       export { EducationPanel } from './EducationPanel';
       export { CommunityPatternsPanel } from './CommunityPatternsPanel';
       export { SecurityTipBanner } from './SecurityTipBanner';
       ```

    2. **Delete `src/components/DashboardFeatures.tsx`**:
       ```
       rm src/components/DashboardFeatures.tsx
       ```

    3. **Verify** that the import in `src/app/dashboard/page.tsx` still resolves correctly:
       ```typescript
       import {
           SecurityScoreWidget,
           AchievementsPanel,
           EducationPanel,
           CommunityPatternsPanel,
           SecurityTipBanner
       } from '@/components/DashboardFeatures';
       ```
       This import path resolves to `src/components/DashboardFeatures/index.ts` via Node module resolution, so it should work without changes.

    4. **Verify TypeScript compilation** to confirm the barrel export resolves correctly:
       ```
       npx tsc --noEmit
       ```
  </action>
  <verify>
    <automated>grep -c "export {" src/components/DashboardFeatures/index.ts | grep -q 5 && echo "Barrel has 5 re-exports"</automated>
    <automated>test ! -f src/components/DashboardFeatures.tsx && echo "Monolithic file removed"</automated>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -10</automated>
    <automated>npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `src/components/DashboardFeatures/index.ts` re-exports all 5 components
    - `src/components/DashboardFeatures.tsx` deleted
    - Import path `@/components/DashboardFeatures` still resolves correctly via barrel
    - All tests pass
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| barrel export → consumer | Re-export of named components; no change to import API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | barrel export index.ts | accept | No logic change — only re-exports existing named exports. TypeScript barrel resolution is a standard pattern. |
</threat_model>

<verification>
- 5 component files in `src/components/DashboardFeatures/`
- `index.ts` barrel export with all 5 re-exports
- `DashboardFeatures.tsx` removed
- `npx tsc --noEmit` — 0 errors
- `npx vitest run` — all tests pass
</verification>

<success_criteria>
- REQ-11 complete: DashboardFeatures.tsx split into 5 focused modules with barrel export
- Zero behavioral change — imports in page.tsx continue to work
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/03-code-quality-architecture-refinement/03-04-SUMMARY.md` when done
</output>
